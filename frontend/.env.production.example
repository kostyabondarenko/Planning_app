# Скопировать в frontend/.env.production
# Читается Next.js на этапе `next build` и инлайнится в бандл.
# После изменения требуется пересборка image:
# docker compose -f docker-compose.prod.yml build frontend --no-cache

NEXT_PUBLIC_API_URL=https://api.goalnavigator.ru
