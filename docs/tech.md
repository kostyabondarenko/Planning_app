# Техническая документация — Goal Navigator MVP

## 1. Технологический стек (актуальные версии)

### Backend
- **FastAPI** — современный веб-фреймворк для Python API
- **SQLAlchemy** 2.0+ — ORM с использованием DeclarativeBase и Mapped types
- **Uvicorn** — ASGI-сервер для запуска FastAPI
- **Pydantic** — валидация данных через схемы
- **PostgreSQL** — основная реляционная база данных
- **psycopg2-binary** — драйвер для PostgreSQL
- **Python-Jose** — работа с JWT-токенами (криптография)
- **Passlib** с bcrypt — безопасное хеширование паролей
- **Python-multipart** — поддержка form-data
- **Python-dotenv** — управление переменными окружения

### Frontend
- **Next.js** 16.1.0+ — React фреймворк с SSR/SSG
- **React** 19.0.0 — библиотека для UI
- **TypeScript** 5.7.0+ — типизированный JavaScript
- **Tailwind CSS** 3.4.0 — utility-first CSS фреймворк
- **Lucide React** 0.470.0 — библиотека иконок
- **ESLint** 9.0.0 — линтер для качества кода

### Тестирование
- **pytest** — фреймворк для unit-тестов
- **SQLite** (in-memory) — база для тестов

### Инфраструктура
- **Docker** — контейнеризация PostgreSQL
- **Docker Compose** — оркестрация контейнеров

## 2. Платформа и интерфейс
- **Клиентское приложение**: веб-приложение на Next.js (Progressive Web App)
- **Desktop‑версия**: веб-приложение работает в любом браузере
- **Мобильная версия**: адаптивная верстка через Tailwind CSS (будущее: нативные приложения)
- **UI-слой**: современный веб‑интерфейс с адаптивной версткой
- **Доступность**: базовая поддержка доступности (контраст, крупные кликабельные элементы)

## 3. Основные модули продукта (реализовано в MVP)
- ✅ **Модуль целей**: хранение и связь целей разных уровней (цель → шаги)
- ✅ **ToDo‑модуль**: быстрые задачи с датами, выполнение, связь с шагами
- ✅ **Модуль прогресса**: расчет и визуализация прогресса целей
- 🚧 **Календарь**: дневной/недельный обзор нагрузки и планов (в разработке)
- ⏳ **AI‑помощник**: планирование, обратная связь, мотивирующий стиль (планируется)
- ⏳ **Голосовой ввод**: преобразование речи в текст для целей и задач (планируется)

## 4. Серверная часть (детали реализации)
- **API‑слой**: FastAPI с автоматической генерацией OpenAPI документации
- **ORM**: SQLAlchemy 2.0 с DeclarativeBase и Mapped types
- **Бизнес‑логика**: 
  - Связь целей и шагов (один-ко-многим)
  - Расчёт прогресса в реальном времени
  - Связывание задач с шагами целей (опционально)
- **Безопасность**:
  - JWT-токены для аутентификации (python-jose)
  - Хеширование паролей через Passlib с bcrypt
  - Timezone-aware даты (UTC)
  - Изоляция данных по user_id
- **Сервис задач**: CRUD операции, фильтрация по датам, статусам

## 5. Данные и хранение

### Модель данных
```
User (пользователь)
├── Goal (цель) ─── многие
│   └── Step (шаг) ─── многие
└── Todo (задача) ─── многие
    └── Step (связь опциональна)
```

### Таблицы базы данных
1. **users**: id, email, hashed_password
2. **goals**: id, user_id (FK), title, description, deadline, status
3. **steps**: id, goal_id (FK), title, is_completed, order
4. **todos**: id, user_id (FK), step_id (FK nullable), title, date, is_completed

### Правила cascading
- При удалении User → удаляются все его Goals и Todos
- При удалении Goal → удаляются все его Steps
- При удалении Step → поле step_id в связанных Todos становится NULL

### Индексы (для производительности)
- users.email (unique, indexed)
- goals.user_id (indexed)
- steps.goal_id (indexed)
- todos.user_id (indexed)

## 6. AI‑слой (планируется в будущем)
- **Промптинг и контекст**: краткое описание пользователя, его целей и стиля общения
- **Роли**: поддержка "хвалить" или "критиковать" по выбору пользователя
- **Безопасность**: запрет на опасные советы, фильтры на чувствительный контент

## 7. Голос и распознавание речи (планируется)
- **ASR‑сервис**: распознавание речи в текст
- **Нормализация**: очистка текста, выделение намерений (цель/задача/дата)

## 8. Интеграции (планируется)
- **Календарь**: импорт/экспорт событий
- **Уведомления**: напоминания о задачах и ключевых этапах

## 9. Безопасность и приватность

### Аутентификация
- **Метод**: JWT (JSON Web Tokens)
- **Хранение токена**: localStorage/httpOnly cookies (frontend)
- **Время жизни токена**: настраивается в переменных окружения
- **Хеширование паролей**: bcrypt через Passlib

### Изоляция данных
- Каждый запрос проверяет `current_user` через JWT
- Все запросы фильтруются по `user_id`
- Невозможно получить доступ к чужим данным
- 404 вместо 403 для скрытия информации о существовании данных

### Защита от атак
- **SQL Injection**: защита через SQLAlchemy ORM
- **XSS**: защита через Pydantic валидацию
- **CSRF**: защита через токены (будущее)

## 10. Тестирование

### Стратегия тестирования
- **Unit-тесты**: проверка бизнес-логики (расчет прогресса, CRUD)
- **Integration тесты**: проверка API endpoints
- **Изоляция**: каждый тест использует отдельную in-memory БД

### Покрытие тестами
- ✅ 100% покрытие критической логики расчета прогресса
- ✅ Все CRUD операции для Goals, Steps, Todos
- ✅ Аутентификация и изоляция данных
- ✅ Edge cases (нет шагов, деление на 0, пустые данные)

### Запуск тестов
```bash
cd backend
pytest -v                    # Все тесты
pytest tests/test_progress.py  # Только прогресс
pytest -k "test_stats"       # Тесты со словом "stats"
```

### Fixtures (conftest.py)
- `session` — изолированная БД для каждого теста
- `client` — TestClient FastAPI
- `auth_client` — клиент с JWT-токеном + тестовый пользователь

## 11. Развертывание

### Локальная разработка
```bash
# Backend
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

# Frontend
cd frontend
npm install
npm run dev

# Database
docker-compose up -d
```

### Переменные окружения
```
# Backend (.env)
DATABASE_URL=postgresql://user:password@localhost/dbname
SECRET_KEY=your-secret-key-for-jwt
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### Структура проекта
```
Planning_app/
├── backend/          # FastAPI приложение
├── frontend/         # Next.js приложение
├── docs/            # Документация
├── plans/           # Планы развития
├── docker-compose.yml
└── README.md
```

### CI/CD (планируется)
- **Среды**: dev / staging / prod
- **Автотесты**: запуск pytest при каждом коммите
- **Деплой**: автоматический деплой на staging после прохождения тестов
