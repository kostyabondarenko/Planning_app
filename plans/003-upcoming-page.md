```markdown
# План: Страница "Ближайшие дни"

## Общее описание

Kanban-доска с колонками-днями для оперативного управления задачами. Связывает ежедневные действия с прогрессом по вехам.

---

## Функциональные требования

### 1. Kanban-доска

**Структура:**
- Горизонтальный скролл колонок
- Каждая колонка = один день
- Первая колонка = сегодня
- Количество колонок: 1–14 (по умолчанию 7)

**Настройки:**
- Селектор количества дней (dropdown или +/- кнопки)
- Сохранение настройки в localStorage

---

### 2. Карточка дня (колонка)

**Заголовок:**
- День недели (Пн, Вт...)
- Дата (6 февраля)
- Подсветка "Сегодня"

**Прогресс:**
- Индикатор выполнения (X из Y задач)
- Прогресс-бар под заголовком

**Список задач:**
- Невыполненные сверху
- Выполненные снизу (визуально приглушены, но не скрыты)

---

### 3. Карточка задачи

**Атрибуты:**
- Название
- Тип: иконка (🔄 регулярная / 📌 однократная)
- Веха (badge с названием)
- Чекбокс/кружок выполнения

**Состояния:**
- Обычная (белый/прозрачный фон)
- Выполненная (зелёный чекбокс, приглушённый текст, перечёркнуто)
- Перетаскиваемая (приподнята, тень)

**Действия:**
- Клик на чекбокс → отметить выполненной
- Drag → перенести на другой день
- Повторный клик на чекбокс → вернуть в работу

---

### 4. Drag & Drop

**Возможности:**
- Перенос задачи между днями
- Визуальный preview при перетаскивании

**При переносе:**
- Регулярная задача: меняется дата в RecurringActionLog
- Однократная задача: меняется deadline

**Ограничения:**
- Нельзя перенести за пределы видимых дней (показать подсказку)

---

### 5. Добавление задачи

**Кнопка:** На каждой карточке дня "+" или общая кнопка

**Форма (модальное окно):**
- Название задачи (обязательно)
- Тип: регулярная / однократная
- Дата (предзаполнена выбранным днём)
- Веха (dropdown из активных вех, обязательно)
- Для регулярной: выбор дней недели

---

### 6. Связь с прогрессом вехи

**При выполнении задачи:**
1. Обновить completed = true
2. Пересчитать прогресс вехи
3. Обновить UI (прогресс-бар дня)
4. Показать toast "Прогресс по вехе X: 45%"

---

## UI/UX детали

### Визуальный стиль (см. brandbook.final.html)

**Доска:**
- Горизонтальный скролл (scroll-snap)
- Фон: var(--bg-secondary)
- Gap между колонками: 16px

**Карточка дня:**
- Glass-эффект (var(--glass-bg), backdrop-filter)
- Ширина: 300px
- Min-height: 400px
- Border-radius: var(--radius-xl)
- Сегодня: подсветка border-color

**Карточка задачи:**
- Background: var(--bg-tertiary)
- Padding: 12px 16px
- Border-radius: var(--radius-md)
- Hover: translateY(-2px), shadow

**Чекбокс:**
- Размер: 20x20px
- Пустой: border 2px var(--border-medium)
- Выполнен: background var(--accent-success), галочка

**Прогресс дня:**
- Высота: 4px
- Фон: var(--border-light)
- Заполнение: var(--kanban-accent)
- 100%: var(--accent-success)

---

## Модель данных

### Используются существующие модели:
- `RecurringAction` — регулярные действия
- `RecurringActionLog` — логи выполнения регулярных
- `OneTimeAction` — однократные действия
- `Milestone` — вехи (для связи)

### TaskView (виртуальная модель для API)
```
id: UUID
type: "recurring" | "one-time"
title: string
date: date
milestone_id: UUID
milestone_title: string
completed: boolean
original_id: UUID  // ID исходного RecurringAction или OneTimeAction
log_id: UUID?      // ID RecurringActionLog (только для регулярных)
```

---

## API Endpoints

### GET /api/tasks/range
Получить задачи за диапазон дат.

**Query params:**
- `start_date`: date (YYYY-MM-DD)
- `end_date`: date (YYYY-MM-DD)

**Response:**
```json
{
  "tasks": [
    {
      "id": "task-view-uuid",
      "type": "recurring",
      "title": "Утренняя пробежка",
      "date": "2026-02-06",
      "milestone_id": "milestone-uuid",
      "milestone_title": "Фитнес Q1",
      "completed": false,
      "original_id": "recurring-action-uuid",
      "log_id": "log-uuid"
    }
  ]
}
```

### PUT /api/tasks/{id}/complete
Отметить задачу выполненной/невыполненной.

**Body:**
```json
{
  "type": "recurring",
  "completed": true,
  "date": "2026-02-06",
  "log_id": "log-uuid"
}
```

**Response:**
```json
{
  "success": true,
  "milestone_progress": 45
}
```

### PUT /api/tasks/{id}/reschedule
Перенести задачу на другой день.

**Body:**
```json
{
  "type": "recurring",
  "old_date": "2026-02-06",
  "new_date": "2026-02-08",
  "log_id": "log-uuid"
}
```

### POST /api/tasks/
Создать новую задачу.

**Body (однократная):**
```json
{
  "type": "one-time",
  "title": "Купить кроссовки",
  "deadline": "2026-02-10",
  "milestone_id": "milestone-uuid"
}
```

**Body (регулярная):**
```json
{
  "type": "recurring",
  "title": "Утренняя пробежка",
  "weekdays": [1, 3, 5],
  "milestone_id": "milestone-uuid"
}
```

---

## Скиллы для CloudCode

### Скилл: brandbook-stylist
**Файл:** `.claude/skills/brandbook-stylist.md`
```
Ты эксперт по стилизации компонентов согласно brandbook.

Знаешь:
- CSS-переменные из brandbook.final.html
- Glass-эффекты, градиенты, тени
- Цветовую палитру (light/dark theme)
- Радиусы, отступы, transitions

Правила:
- Используй только CSS-переменные из :root
- Поддерживай обе темы (light/dark)
- Следуй паттернам glass-card, task-card
- Применяй hover/active состояния

Не занимаешься: бизнес-логикой, API, state management
```

### Скилл: fastapi-crud
**Файл:** `.claude/skills/fastapi-crud.md`
```
Ты эксперт по созданию FastAPI endpoints.

Знаешь:
- Структуру backend/app/
- Модели SQLAlchemy (models.py)
- Pydantic-схемы (schemas.py)
- Паттерны роутеров (routers/)
- JWT-аутентификацию (auth.py)

Правила:
- Все endpoints требуют current_user
- Фильтруй по user_id
- Возвращай 404 вместо 403
- Используй async где возможно
- Пиши Pydantic-схемы для request/response

Не занимаешься: frontend, стилями
```

### Скилл: dnd-kanban
**Файл:** `.claude/skills/dnd-kanban.md`
```
Ты эксперт по drag & drop с dnd-kit.

Знаешь:
- @dnd-kit/core, @dnd-kit/sortable
- DndContext, DragOverlay
- useDraggable, useDroppable
- Sensors (pointer, keyboard)
- Collision detection

Правила:
- Используй DragOverlay для preview
- Обрабатывай onDragEnd для API-вызовов
- Добавляй aria-атрибуты для accessibility
- Оптимизируй рендеринг (memo)

Не занимаешься: стилями (только структура), API
```

### Скилл: react-forms
**Файл:** `.claude/skills/react-forms.md`
```
Ты эксперт по React-формам и модальным окнам.

Знаешь:
- Controlled components
- Form validation
- useState/useReducer для форм
- Модальные окна (portal, focus trap)
- Error handling, loading states

Правила:
- Валидируй на клиенте перед отправкой
- Показывай inline-ошибки
- Блокируй кнопку при отправке
- Закрывай модалку по Escape и клику вне

Не занимаешься: drag & drop, сложными стилями
```

---

## План реализации

### Этап 1: Дополнить Brandbook стилями Kanban
**Статус:** ✅ Выполнен

**Промпт для CloudCode:**
```
Выполни Этап 1 из plans/003-upcoming-page.md.

Используй скилл: brandbook-stylist

Добавь в brandbook.final.html стили для Kanban-доски:
- .kanban-board (контейнер с горизонтальным скроллом)
- .day-column (колонка дня, glass-эффект)
- .day-column.today (подсветка сегодняшнего дня)
- .day-header (заголовок с датой)
- .day-progress (прогресс-бар)
- .task-card (карточка задачи)
- .task-card.completed (выполненная)
- .task-card.dragging (при перетаскивании)
- .task-checkbox (кружок выполнения)
- .task-checkbox.checked (выполнен)

Следуй существующим паттернам из brandbook.
```

**Результат:**
- [x] Стили .kanban-board
- [x] Стили .day-column и .day-column.today
- [x] Стили .day-header и .day-progress
- [x] Стили .task-card (обычная, completed, dragging)
- [x] Стили .task-checkbox

---

### Этап 2: Backend — API для задач
**Статус:** ✅ Выполнен

**Промпт для CloudCode:**
```
Выполни Этап 2 из plans/003-upcoming-page.md.

Используй скилл: fastapi-crud

Создай API endpoints в backend/app/routers/tasks.py:

1. GET /api/tasks/range?start_date=X&end_date=Y
   - Собрать регулярные задачи (по weekdays и диапазону)
   - Собрать однократные задачи (по deadline)
   - Объединить в TaskView формат
   - Вернуть с информацией о вехе

2. PUT /api/tasks/{id}/complete
   - Для регулярных: обновить/создать RecurringActionLog
   - Для однократных: обновить OneTimeAction.completed
   - Пересчитать прогресс вехи
   - Вернуть новый прогресс

3. PUT /api/tasks/{id}/reschedule
   - Для регулярных: изменить дату в RecurringActionLog
   - Для однократных: изменить deadline

4. POST /api/tasks/
   - Создать RecurringAction или OneTimeAction
   - Привязать к milestone_id

Создай Pydantic-схемы в schemas.py.
Добавь роутер в main.py.
```

**Результат:**
- [x] Файл routers/tasks.py
- [x] Схемы TaskView, TaskComplete, TaskReschedule, TaskCreate
- [x] GET /api/tasks/range
- [x] PUT /api/tasks/{id}/complete
- [x] PUT /api/tasks/{id}/reschedule
- [x] POST /api/tasks/

---

### Этап 3: Frontend — Базовая структура Kanban
**Статус:** ✅ Выполнен

**Промпт для CloudCode:**
```
Выполни Этап 3 из plans/003-upcoming-page.md.

Создай базовую структуру страницы Kanban БЕЗ drag & drop.

Файлы:
- frontend/src/app/dashboard/upcoming/page.tsx
- frontend/src/components/kanban/KanbanBoard.tsx
- frontend/src/components/kanban/DayColumn.tsx
- frontend/src/components/kanban/TaskCard.tsx

Требования:
1. KanbanBoard:
   - Загрузка задач через GET /api/tasks/range
   - Хранение количества дней (1-14, default 7)
   - Селектор количества дней
   - Горизонтальный скролл

2. DayColumn:
   - Заголовок (день недели, дата)
   - Подсветка "Сегодня"
   - Прогресс-бар (выполнено/всего)
   - Список задач (невыполненные сверху)

3. TaskCard:
   - Название, тип (иконка), веха (badge)
   - Чекбокс выполнения
   - Клик на чекбокс → PUT /api/tasks/{id}/complete
   - Визуальное состояние completed

Используй стили из brandbook (CSS-переменные).
Сохраняй количество дней в localStorage.
```

**Результат:**
- [x] Страница /dashboard/upcoming
- [x] Компонент KanbanBoard
- [x] Компонент DayColumn
- [x] Компонент TaskCard
- [x] Селектор количества дней
- [x] Чекбокс выполнения работает

---

### Этап 4: Frontend — Drag & Drop
**Статус:** ✅ Выполнен

**Промпт для CloudCode:**
```
Выполни Этап 4 из plans/003-upcoming-page.md.

Используй скилл: dnd-kanban

Добавь drag & drop в Kanban-доску:

1. Установи @dnd-kit/core, @dnd-kit/sortable (если не установлены)

2. Обнови KanbanBoard:
   - Оберни в DndContext
   - Добавь sensors (pointer, keyboard)
   - Обработай onDragEnd

3. Обнови DayColumn:
   - useDroppable для колонки
   - Подсветка при наведении draggable

4. Обнови TaskCard:
   - useDraggable
   - Стили .dragging при перетаскивании

5. Добавь DragOverlay:
   - Показывать превью TaskCard при drag

6. При drop на другой день:
   - Вызвать PUT /api/tasks/{id}/reschedule
   - Обновить локальный state
   - Показать toast об успехе/ошибке
```

**Результат:**
- [x] DndContext в KanbanBoard
- [x] useDroppable в DayColumn
- [x] useDraggable в TaskCard
- [x] DragOverlay с превью
- [x] API-вызов при переносе
- [x] Обновление UI после drop

---

### Этап 5: Frontend — Форма добавления задачи
**Статус:** ✅ Выполнен

**Промпт для CloudCode:**
```
Выполни Этап 5 из plans/003-upcoming-page.md.

Используй скилл: react-forms

Создай форму добавления задачи:

1. Компонент AddTaskModal (frontend/src/components/kanban/AddTaskModal.tsx):
   - Модальное окно (portal)
   - Закрытие по Escape и клику вне
   - Focus trap

2. Поля формы:
   - Название (text, required)
   - Тип (radio: регулярная / однократная)
   - Дата (date picker, предзаполнена)
   - Веха (dropdown, required, загрузить активные)
   - Дни недели (checkboxes, только для регулярных)

3. Валидация:
   - Название не пустое
   - Веха выбрана
   - Для регулярных: хотя бы один день недели

4. Отправка:
   - POST /api/tasks/
   - Loading state на кнопке
   - Закрыть модалку при успехе
   - Обновить список задач
   - Toast "Задача добавлена"

5. Кнопка "+" на каждой DayColumn:
   - Открывает модалку
   - Передаёт дату колонки
```

**Результат:**
- [x] Компонент AddTaskModal
- [x] Форма с валидацией
- [x] Dropdown вех (загрузка с API)
- [x] Выбор дней недели для регулярных
- [x] Кнопка "+" на колонках
- [x] Интеграция с API

---

### Этап 6: Полировка и тесты
**Статус:** ✅ Выполнен

**Промпт для CloudCode:**
```
Выполни Этап 6 из plans/003-upcoming-page.md.

Финальная полировка страницы "Ближайшие дни":

1. Loading states:
   - Skeleton для KanbanBoard при загрузке
   - Skeleton для DayColumn

2. Error states:
   - Ошибка загрузки задач
   - Ошибка при выполнении/переносе
   - Retry кнопка

3. Empty states:
   - Нет задач на день
   - Нет задач вообще (с кнопкой создания)

4. Toast notifications:
   - Задача выполнена (+ прогресс вехи)
   - Задача перенесена
   - Задача добавлена
   - Ошибки

5. Мобильная адаптация:
   - Горизонтальный свайп
   - Колонки 280px на мобильных
   - Touch-friendly чекбоксы (44x44 hit area)

6. Backend тесты (pytest):
   - test_get_tasks_range
   - test_complete_task
   - test_reschedule_task
   - test_create_task
```

**Результат:**
- [x] Skeleton loading
- [x] Error handling
- [x] Empty states
- [x] Toast notifications
- [x] Мобильная адаптация
- [x] Backend тесты

---

## Чеклист готовности

### Функциональность
- [ ] Kanban-доска отображает дни
- [ ] Задачи загружаются и отображаются
- [ ] Чекбокс выполнения работает
- [ ] Drag & drop между днями работает
- [ ] Форма добавления задачи работает
- [ ] Прогресс вехи обновляется
- [ ] Настройка количества дней сохраняется

### UI/UX
- [ ] Стили соответствуют brandbook
- [ ] Поддержка light/dark темы
- [ ] Loading/error/empty states
- [ ] Мобильная адаптация
- [ ] Анимации и transitions

### Качество
- [ ] Backend тесты проходят
- [ ] Нет console errors
- [ ] Нет TypeScript ошибок
```